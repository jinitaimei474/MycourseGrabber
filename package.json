{
  "name": "sjtu-course-timer",
  "version": "0.6.0",
  "private": true,
  "scripts": {
    "test": "node --test test/core.test.cjs",
    "check": "node --check course-timer.user.js && node --check extension-loader.js"
  },
  "engines": {
    "node": ">=18"
  }
}
